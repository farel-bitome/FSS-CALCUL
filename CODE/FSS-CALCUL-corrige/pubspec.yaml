name: fss_calcul
description: FSS-CALCUL - caisse Android avec impression multi-terminaux
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
  # Plugin communautaire (éditeur non vérifié) — à tester sur un vrai terminal Senraise
  senraise_printer: ^0.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/fss_logo.png
